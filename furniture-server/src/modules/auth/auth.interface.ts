export interface ITokens {
    access_token: string;
    refresh_token: string;
}

export interface IUserRegister {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
}