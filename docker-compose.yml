services:
  db:
    image: "postgres:16"
    restart: always
    container_name: "db"
    env_file:
      - ./furniture-server/.env
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data: